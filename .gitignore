# LaTeX intermediate and output files
*.aux
*.lof
*.lot
*.log
*.fls
*.fdb_latexmk
*.out
*.toc
*.bbl
*.blg
*.synctex.gz
*.nav
*.snm
*.vrb
*.xdv
*.xdv-conversion
*.dvi
*.ps
*.pdfsync

# Editor backups and temp files
*~
*.swp
*.swo
.DS_Store
Thumbs.db

# Build directories
_build/
build/

# Images and large datasets
# By default Images/ is tracked (keep figures required for the thesis in the repo).
# If you have large raw microscopy datasets, keep them outside the repo or use Git LFS
# and optionally ignore a raw subfolder (example below):
# Images/raw/

# Bibliography and auxiliary LaTeX files generated by tools (keep .bib source if needed)
Thesis.blg
Thesis.bcf

# Python virtual envs, caches (if any code exists)
venv/
.env
__pycache__/
*.pyc

# Jupyter notebooks checkpoints
.ipynb_checkpoints/

# Git LFS pointer files (if using Git LFS for large files)
# Add patterns of large binaries you plan to store with LFS

# Misc
*.bak
*.backup

# Keep the source .tex and bibliography file
!Thesis.tex
!Configuration_Files/**
!Thesis_bibliography.bib
